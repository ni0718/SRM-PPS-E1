#include <stdio.h>

int main() 
{
    int giv,mp,q,r;
    printf("Enter the money given by Santo\n");scanf("%d",&giv);
    printf("Enter the money to be paid to the shopkeeper\n");scanf("%d",&mp);
    q=giv/mp;r=giv%mp;
    printf("Quotient = %d\nRemainder = %d",q,r);
    return 0;
}    
